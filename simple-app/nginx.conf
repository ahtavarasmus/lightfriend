server {
    listen 80;

    location / {
        root /usr/share/nginx/html;
        index index.html;
    }

    location /api/ {
        proxy_pass http://core:3000;
        proxy_http_version 1.1;
    }
}
